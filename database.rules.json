{
  "rules": {
    "users": {
      "$user_id": {
        ".write": "$user_id === auth.uid"
      }
    },
    "sessions": {
      ".indexOn": ["creator_uid"]
    },
    "issues": {
      ".indexOn": ["session_uid"]
    },
      ".read": "auth !== null",
        ".write": "auth !== null"
  }
}
